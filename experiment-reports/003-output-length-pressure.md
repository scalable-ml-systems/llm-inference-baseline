Longer generations create longer-lived sequences.

This can reduce available scheduling capacity for newer requests and increase tail latency even when the backend still appears productive in tokens/sec.