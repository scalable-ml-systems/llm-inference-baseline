The mixed workload showed that backend-level average throughput is insufficient as a routing signal.

Short interactive requests can suffer behind long-prefill or long-decode requests.
This motivates future routing policies that account for queue depth, request shape, and backend state.