In this build, the router and Kubernetes scheduler are intentionally out of scope.

The purpose is to characterize a single backend deeply enough to define what future schedulers and routers should optimize.

